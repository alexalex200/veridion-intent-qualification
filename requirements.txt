numpy>=1.24
scikit-learn>=1.3

# LLM verification (llm_check.py) is optional and talks to a local Ollama
# server via its REST API (no extra pip package needed for that part).
# Install Ollama separately (https://ollama.com) and run
# `ollama pull llama3.2:3b` to enable it. Without Ollama running, or with
# --no-llm, the script just skips that step and uses the rule+embedding
# scoring only.
