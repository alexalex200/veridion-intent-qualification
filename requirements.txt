numpy>=1.24
scikit-learn>=1.3

# Optional, higher-quality semantic backend. Not required to run
# solution.py (TF-IDF is the default and needs neither of these).
# Install and set QUALIFIER_USE_ST=1 to enable:
# sentence-transformers>=2.2

# Optional LLM verification stage (qualifier/llm_verifier.py) talks to a
# local Ollama server over its REST API using only the stdlib (urllib) -
# no pip package required. Install Ollama separately (https://ollama.com)
# and `ollama pull llama3.2:3b` to enable it; the pipeline runs fine
# without it (rule+embedding scoring only) if Ollama isn't reachable, or
# with --no-llm to disable it explicitly.
