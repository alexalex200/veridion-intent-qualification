numpy>=1.24
scikit-learn>=1.3

# Optional, higher-quality semantic backend. Not required to run
# solution.py (TF-IDF is the default and needs neither of these).
# Install and set QUALIFIER_USE_ST=1 to enable:
# sentence-transformers>=2.2
